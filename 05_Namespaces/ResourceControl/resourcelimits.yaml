apiVersion: v1
kind: LimitRange
metadata:
  name: dev-team-limitrange
  namespace: dev-team
spec:
  limits:
  - type: Pod
    max:
      cpu: 150m
      memory: 250Mi
    min:
      cpu: 100m
      memory: 200Mi
