apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dynamic-disk-pvc
spec:
  storageClassName: custom-storageclass
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi