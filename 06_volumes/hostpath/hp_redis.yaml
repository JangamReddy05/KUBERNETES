apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis-app
spec:
  replicas: 5
  selector:
    matchLabels:
      app: redis-app
  template:
    metadata:
      labels:
        app: redis-app
    spec:
      containers:
      - name: redis-app
        image: redis
        ports:
        - containerPort: 6379
        volumeMounts:
        - name: hostpath-volume
          mountPath: /redis-data
      volumes:
      - name: hostpath-volume
        hostPath:
          path: /tmp/redis-data