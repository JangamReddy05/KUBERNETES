apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrpt-staging
  namespace: cert-manager
spec:
  acme:
    server: https://acme-staging-v02.api.letsencrypt.org/directory
    email: mahidevops.in
    privateKeySecretRef:
      name: letsencrpt-staging
    solvers:
    - http01:
        ingress:
          name: nginx
